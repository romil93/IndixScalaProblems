/**
 * Created by indix on 23/2/15.
 */

class RemoteControl {
  def keyPresses(minChannel: Int, maxChannel: Int, numberOfBlockedChannels: Int, blockedChannels: List[Int], numberOfChannelsToView: Int, channelsToView: List[Int]): Int = {
    def upKeyMoves(fromChannel: Int, toChannel: Int): Int = {
      
    }
  }
}
